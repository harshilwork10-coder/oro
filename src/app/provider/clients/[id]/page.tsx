'use client';

// This page embeds the existing client details from the old dashboard
// The old page has full functionality: real data fetch, documents, edit modal, business info

import ClientDetailsPage from '@/app/dashboard/franchisors/[id]/page';

export default function ProviderClientDetailPage() {
    return <ClientDetailsPage />;
}
